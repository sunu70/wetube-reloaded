extends base

block content 
    form(method="POST")
name(name="name", type="text",required) 
email(name="email", type="text",required)
username(name="username ", type="text",required) 
password(name="password", type="text",required)
location(name="location", type="text",required)
input(type="submit", value="Join")